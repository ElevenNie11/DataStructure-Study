#include <stdio.h>
#include <stdlib.h>

typedef struct Node{
    int data;
    struct Node* next;
}Node;

// 创建链表的函数 返回头节点
// 尾插法
Node* createList(){
    Node* head = NULL;
    Node* tail = NULL;
    
}


int main(){

    return 0;
}